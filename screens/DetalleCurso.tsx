import React from "react";
import { View,Button,Text } from 'react-native';

export default ({ navigation }) => {

  return (
    <View>
      {/* <Button title='Ir a Home' onPress={()=> navigation.navigate('Home')} /> */}
      <Text>Detalle Cursos.tsx</Text>
    </View>
  )
}